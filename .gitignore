# 凭据与本地配置，严禁入库
ACCOUNTS.json
# 通配：手工/脚本备份常带时间戳（ACCOUNTS.json.bak.20260729_001953），
# 精确匹配 ACCOUNTS.json.bak 覆盖不到，而这类文件含全部账号的 token 与明文密码。
ACCOUNTS.json.bak*
ACCOUNTS.json.lock

# 临时诊断脚本（会读取账号密码做登录实验，不入库）
.tmpdiag/
.tmpchk/

# 运行产物（results/ 是旧目录名，保留忽略以免历史遗留文件被误提交）
.cache-checkin/
results/

# 验证码真实样本（仅用于本地验收准确率；1.5MB 二进制掩码，不入库）
# 模板库 captcha_ocr/templates.npz 是构建产物、体积仅 34KB，照常跟踪。
captcha_ocr/data/real_samples.json
# base64Captcha 的 9 个第三方 TTF（660KB，许可未逐一确认，不随本仓库分发）。
# 只有重建 captcha_ocr/base64_templates.npz 时才需要，运行期只读已打包的 npz；
# 下载地址与缺失时的提示见 captcha_ocr/base64_captcha.py 的 FONTS_URL。
captcha_ocr/fonts/
login_grant_state.json
checkin_report.md
*.lock
!uv.lock
__pycache__/
*.pyc

# 浏览器持久化登录态（含 Linux.do/OAuth 会话，严禁入库）
.browser_profile/

# Python 虚拟环境 / 构建产物
.venv/
*.egg-info/
build/
dist/
.narrafork/
.worktrees/
